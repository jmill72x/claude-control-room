.DS_Store
node_modules/
dist/

# Redundant with the extracted design_handoff_* folder
Claude dashboard prototype.zip

# Local runtime state — machine-specific, not source
server/config.json
server/todos.json

# Usage history: personal, non-recoverable, and this repo is public
server/data/

# The real plist carries absolute paths; only the example is tracked
deploy/*.plist
!deploy/*.plist.example
